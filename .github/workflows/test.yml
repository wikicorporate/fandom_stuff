name: Test Wall Post

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - run: pip install mwclient requests
      - name: Run test
        env:
          MAIN_ACCOUNT_USER: ${{ secrets.MAIN_ACCOUNT_USER }}
          MAIN_ACCOUNT_PASSWORD: ${{ secrets.MAIN_ACCOUNT_PASSWORD }}
          TEST_USER: "Swit4er"
        run: python src/scripts/bots/test_post.py
